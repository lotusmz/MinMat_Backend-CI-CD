name: CI/CD Pipeline - MindMat Backend

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
    types: [opened, synchronize, reopened]

jobs:
  build-and-test:
    name: Build, Test & Quality Analysis
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout del código
      - name: 📥 Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Necesario para SonarQube análisis completo

      # 2. Configurar JDK
      - name: ☕ Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      # 3. Cache de dependencias Maven
      - name: 📦 Cache Maven packages
        uses: actions/cache@v3
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-m2

      # 4. Compilar el proyecto
      - name: 🔨 Build with Maven
        run: mvn clean compile -DskipTests

      # 5. Ejecutar pruebas unitarias
      - name: 🧪 Run Unit Tests
        run: mvn test

      # 6. Generar reporte de cobertura
      - name: 📊 Generate Coverage Report
        run: mvn jacoco:report

      # 7. Análisis con SonarQube/SonarCloud
      - name: 🔍 SonarQube Analysis
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
        run: |
          mvn sonar:sonar \
            -Dsonar.projectKey=mindmat-backend \
            -Dsonar.projectName="MindMat Backend" \
            -Dsonar.host.url=${{ secrets.SONAR_HOST_URL }} \
            -Dsonar.login=${{ secrets.SONAR_TOKEN }} \
            -Dsonar.java.coveragePlugin=jacoco \
            -Dsonar.coverage.jacoco.xmlReportPaths=target/site/jacoco/jacoco.xml

      # 8. Publicar resultados de tests
      - name: 📈 Publish Test Results
        uses: EnricoMi/publish-unit-test-result-action@v2
        if: always()
        with:
          files: |
            target/surefire-reports/*.xml

      # 9. Subir reporte de cobertura
      - name: 📤 Upload Coverage Report
        uses: actions/upload-artifact@v3
        if: always()
        with:
          name: coverage-report
          path: target/site/jacoco/

      # 10. Verificar Quality Gate de SonarQube
      - name: ✅ SonarQube Quality Gate Check
        uses: sonarsource/sonarqube-quality-gate-action@master
        timeout-minutes: 5
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
        continue-on-error: false

      # 11. Package (solo si todos los tests pasan)
      - name: 📦 Package Application
        if: success()
        run: mvn package -DskipTests

      # 12. Guardar artefacto JAR
      - name: 💾 Upload JAR Artifact
        if: success()
        uses: actions/upload-artifact@v3
        with:
          name: mindmat-backend-jar
          path: target/*.jar
          retention-days: 7

  # Job adicional para notificaciones
  notify:
    name: 📬 Notify Results
    needs: build-and-test
    runs-on: ubuntu-latest
    if: always()

    steps:
      - name: 📧 Build Status
        run: |
          if [ "${{ needs.build-and-test.result }}" == "success" ]; then
            echo "✅ Pipeline ejecutado exitosamente"
          else
            echo "❌ Pipeline falló - revisa los logs"
            exit 1
          fi